%%  spop_app
%%
%% >-----------------------------------------------------------------------< %%

-module(spop_app).

-export([start/2, prep_stop/1, stop/1]).

-behaviour(application).

%% >-----------------------------------------------------------------------< %%

start(_, _) ->
    spop_sup:start_link().

prep_stop(State) ->
    State.

stop(_) ->
    ok.

%% >-----------------------------------------------------------------------< %%
